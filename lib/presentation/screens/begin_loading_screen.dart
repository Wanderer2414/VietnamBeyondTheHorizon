import 'package:flutter/material.dart';

class BeginLoadingScreen extends StatelessWidget {
  const BeginLoadingScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return MaterialApp();
  }
}
