import 'package:flutter/material.dart';

class SubmissionZone extends StatelessWidget {
  const SubmissionZone({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 100,
      decoration: BoxDecoration(color: Colors.grey[100]),
    );
  }
}
